## 🧠 Challenge №1 — співбесіда

"""
Не запускаючи код, скажи результат:

for number in range(5):
    if number == 3:
        break
    print(number)
else:
    print("Finished")

Питання:
Чому Finished не виводиться?
"""

"""
Відповідь — буде виведено:
0
1
2

"""
# Покрокове виконання:
for number in range(5):   # number = 0, 1, 2, 3, 4
    if number == 3:
        break
    print(number)

"""
# Крок 1: number = 0 → 0 == 3? Ні → print(0)
# Крок 2: number = 1 → 1 == 3? Ні → print(1)
# Крок 3: number = 2 → 2 == 3? Ні → print(2)
# Крок 4: number = 3 → 3 == 3? ТАК → break ← цикл зупинено ТУТ
# Крок 5: number = 4 → ніколи не досягнуто
Чому Finished не виводиться:

else у циклі виконується тільки якщо цикл завершився природно — тобто пройшов усі елементи до кінця без break. Як тільки break спрацьовує — блок else повністю пропускається:

# Правило for/else:
for ...:
    break      # ← спрацював break
else:
    ...        # ← ПРОПУСКАЄТЬСЯ

for ...:
    pass       # ← break НЕ спрацював, цикл дійшов до кінця
else:
    ...        # ← ВИКОНУЄТЬСЯ ✅
"""

# Порівняння двох сценаріїв:
# Сценарій 1 — break спрацьовує (наш код):
for number in range(5):
    if number == 3:
        break       # ← є break → else пропускається
    print(number)
else:
    print("Finished")   # ❌ не виводиться
# Вивід: 0, 1, 2

# Сценарій 2 — break НЕ спрацьовує:
for number in range(3):   # range(3) = 0, 1, 2 — немає числа 3
    if number == 3:
        break       # ← умова ніколи не True
    print(number)
else:
    print("Finished")   # ✅ виводиться
# Вивід: 0, 1, 2, Finished

"""
Практичне застосування for/else — саме тому цю конструкцію використовують для пошуку: якщо знайшли — break (і else не виконується), якщо не знайшли — else виконується і сигналізує про відсутність:
"""
patients = ["Ivan", "Olena", "Petro"]

for patient in patients:
    if patient == "Dmytro":
        print("Знайдено!")
        break
else:
    print("Пацієнта не знайдено")   # ← виконається, бо break не спрацював

# ==============================================================================
# ==============================================================================

## 🧠 Challenge №2

"""
Що буде?

numbers = [1, 2, 3, 4, 5]

for number in numbers:
    if number % 2 == 0:
        continue

    print(number)
"""

"""
Відповідь — буде виведено лише непарні числа:
1
3
5
"""

# Покрокове виконання:
numbers = [1, 2, 3, 4, 5]
for number in numbers:
    if number % 2 == 0:
        continue
    print(number)

"""
# Крок 1: number = 1 → 1 % 2 == 0? Ні (остача 1) → print(1)
# Крок 2: number = 2 → 2 % 2 == 0? ТАК (парне) → continue ← пропускаємо print, йдемо на початок циклу
# Крок 3: number = 3 → 3 % 2 == 0? Ні → print(3)
# Крок 4: number = 4 → 4 % 2 == 0? ТАК (парне) → continue ← пропускаємо print
# Крок 5: number = 5 → 5 % 2 == 0? Ні → print(5)

Чому так відбувається:
Вираз number % 2 == 0 перевіряє, чи ділиться число на 2 без остачі (тобто, чи є воно парним).

Якщо умова виконується, спрацьовує continue. Він каже Python: "Кидай все, що нижче в цьому блоці, і переходь до наступного елемента списку".

Відповідно, print(number) для чисел 2 та 4 просто не встигає виконатися.
"""

#Порівняння: continue vs звичайний if

# ✅ З continue (Підхід "Відсіюємо непотрібне"):
for number in numbers:
    if number % 2 == 0:
        continue        # ← рання відмова для парних
    print(number)

# ✅ Без continue (Підхід "Шукаємо лише потрібне"):
for number in numbers:
    if number % 2 != 0: # ← інвертована умова (якщо НЕ парне)
        print(number)

"""
Обидва варіанти дають результат 1, 3, 5. Використання continue стає особливо зручним, коли після перевірки у вас йде не один print, а 20 рядків складної логіки обробки даних (наприклад, аналіз медичного знімку) — це рятує від глибокої вкладеності коду.
"""

"""
┌────────────────────────────────────────┐
│           ФІЛЬТРАЦІЯ ЧИСЕЛ             │
├────────────────────────────────────────┤
│  ✅ Опрацьовано непарне: 1             │
│  ⏭ Пропущено: 2 (парне)                │
│  ✅ Опрацьовано непарне: 3             │
│  ⏭ Пропущено: 4 (парне)                │
│  ✅ Опрацьовано непарне: 5             │
└────────────────────────────────────────┘
"""

# ==============================================================================
# ==============================================================================

## 🧠 Challenge №3

"""
Знайди помилку:

count = 1

while count <= 5:
    print(count)

Що станеться при запуску?
Як це виправити?
"""

"""
Відповідь — станеться безкінечний цикл (infinite loop)! Програма "зависне" і буде нескінченно друкувати одиницю, поки ви її не зупините примусово (наприклад, комбінацією Ctrl+C).
"""

# Покрокове виконання помилкового коду:
count = 1
while count <= 5:
    print(count)

"""
# Крок 1: count = 1 → 1 <= 5? ТАК → print(1)
# Крок 2: count досі 1 → 1 <= 5? ТАК → print(1)
# Крок 3: count досі 1 → 1 <= 5? ТАК → print(1)
# ...
# Крок 1000000: count = 1 → 1 <= 5? ТАК → print(1)
# 💥 Програма зависла, процесор навантажений на 100%.

Чому так? Ми задали умову count <= 5, але всередині циклу значення змінної count ніколи не змінюється. Вона завжди дорівнює 1.

Як це виправити?
Потрібно додати крок зміни (інкременту) нашої змінної всередині тіла циклу, щоб умова колись стала False:
"""

# ✅ Виправлений варіант:
count = 1
while count <= 5:
    print(count)
    count += 1    # ← ДОДАНО: збільшуємо count на 1 при кожній ітерації

"""
Примітка: count += 1 — це скорочений і більш "pythonic" запис для count = count + 1.

Покрокове виконання виправленого коду:
# Крок 1: count = 1 → 1 <= 5? ТАК → print(1) → count стає 2
# Крок 2: count = 2 → 2 <= 5? ТАК → print(2) → count стає 3
# Крок 3: count = 3 → 3 <= 5? ТАК → print(3) → count стає 4
# Крок 4: count = 4 → 4 <= 5? ТАК → print(4) → count стає 5
# Крок 5: count = 5 → 5 <= 5? ТАК → print(5) → count стає 6
# Крок 6: count = 6 → 6 <= 5? НІ 🛑 → цикл завершується!

🩺 Практичний нюанс: Безкінечні цикли — це один із найстрашніших багів у програмуванні. Якщо такий цикл потрапить у код медичного обладнання або інтерфейс програми, програма перестане реагувати на дії користувача, оскільки процесор буде повністю зайнятий "ходінням по колу". Завжди перевіряйте, чи є у вашого while умова для виходу (або break)!
"""

"""
┌────────────────────────────────────────────┐
│         БЕЗПЕЧНЕ ВИКОНАННЯ WHILE           │
├────────────────────────────────────────────┤
│  ✅ Ітерація 1: виведено 1                 │
│  ✅ Ітерація 2: виведено 2                 │
│  ✅ Ітерація 3: виведено 3                 │
│  ✅ Ітерація 4: виведено 4                 │
│  ✅ Ітерація 5: виведено 5                 │
│  🛑 Зупинка: count = 6 (> 5)               │
└────────────────────────────────────────────┘
"""

# ==============================================================================
# ==============================================================================

## 🚀 Challenge №4 — MedAssistant

"""
Створи:
temperatures = [
    36.6,
    37.2,
    38.1,
    39.0,
    37.8,
    36.4,
]

Програма повинна пройти по всіх температурах і визначити:

< 37.5      → Normal
37.5–38.9   → Fever
39+         → High fever

Приклад:
36.6 → Normal
37.2 → Normal
38.1 → Fever
39.0 → High fever
37.8 → Fever
36.4 → Normal
"""

"""
Ми використаємо конструкцію if-elif-else. Це ідеальний інструмент для маршрутизації (тріажу), коли умова має кілька взаємовиключних варіантів.
"""

#💻 Код програми:
temperatures = [36.6, 37.2, 38.1, 39.0, 37.8, 36.4]

for temp in temperatures:
    if temp < 37.5:
        print(f"{temp:.1f} → Normal")
    elif temp < 39.0:
        print(f"{temp:.1f} → Fever")
    else:
        print(f"{temp:.1f} → High fever")

"""
⚙️ Покрокове виконання (як мислить Python):
# Крок 1: temp = 36.6
# 36.6 < 37.5? ТАК → друкуємо "Normal", інші перевірки ігноруємо.

# Крок 3: temp = 38.1
# 38.1 < 37.5? Ні. 
# Йдемо в elif: 38.1 < 39.0? ТАК → друкуємо "Fever".

# Крок 4: temp = 39.0
# 39.0 < 37.5? Ні. 
# Йдемо в elif: 39.0 < 39.0? Ні (вони рівні). 
# Йдемо в else (всі інші випадки) → друкуємо "High fever".

🧠 Чому код написаний саме так? (Best Practices)
Зверніть увагу на рядок elif temp < 39.0:.
Новачки часто пишуть складну умову: elif temp >= 37.5 and temp < 39.0:.
Але в Python порядок має значення.

Оскільки програма читає код зверху вниз, до перевірки elif вона дійде ТІЛЬКИ у тому випадку, якщо if temp < 37.5 виявилося False (тобто температура вже гарантовано 37.5 або вища). Тому нам достатньо перевірити лише верхню межу. Це робить код чистішим і швидшим!
"""

#📊 Фірмовий вивід у рамці:
#Для справжнього MedAssistant зробимо красивий звіт з візуальними маркерами стану:

rows = []
for temp in temperatures:
    if temp < 37.5:
        rows.append(f"✅ {temp:.1f} °C  → Normal")
    elif temp < 39.0:
        rows.append(f"⚠️ {temp:.1f} °C  → Fever")
    else:
        rows.append(f"🚨 {temp:.1f} °C  → High fever")

print("\n┌──────────────────────────────────────┐")
print("│       MEDASSISTANT: ТРІАЖ            │")
print("├──────────────────────────────────────┤")
for row in rows:
    print(f"│  {row.ljust(34)}  │")
print("└──────────────────────────────────────┘")


"""
┌──────────────────────────────────────┐
│       MEDASSISTANT: ТРІАЖ            │
├──────────────────────────────────────┤
│  ✅ 36.6 °C  → Normal                  │
│  ✅ 37.2 °C  → Normal                  │
│  ⚠️ 38.1 °C  → Fever                   │
│  🚨 39.0 °C  → High fever              │
│  ⚠️ 37.8 °C  → Fever                   │
│  ✅ 36.4 °C  → Normal                  │
└──────────────────────────────────────┘
"""

# ==============================================================================
# ==============================================================================

## 🏥 Challenge №5 — Junior+

"""
Створи:
patients = [
    ("Ivan", 36.6),
    ("Olena", 38.2),
    ("Petro", 39.1),
    ("Hanna", 37.0),
]

Використовуючи for та enumerate(), виведи таблицю:
#   Patient   Temperature   Status
1   Ivan      36.6          Normal
2   Olena     38.2          Fever
3   Petro     39.1          High fever
4   Hanna     37.0          Normal

Тут тобі доведеться одночасно використати:
enumerate()
for
if / elif / else
f-string
"""

# Variant #1
"""
Ця задача чудово поєднує одразу кілька важливых концепцій Python. Щоб таблиця виглядала рівною, ми використаємо магію форматування рядків (f-strings) з вирівнюванням.
"""

#💻 Код програми:
patients = [
    ("Ivan", 36.6),
    ("Olena", 38.2),
    ("Petro", 39.1),
    ("Hanna", 37.0),
]

# Друкуємо заголовок таблиці
print(f"{'#':<3} {'Patient':<9} {'Temperature':<13} {'Status'}")

# enumerate з параметром start=1 одразу нумерує з одиниці
for index, (name, temp) in enumerate(patients, start=1):
    
    # Визначаємо статус за допомогою if/elif/else
    if temp < 37.5:
        status = "Normal"
    elif temp < 39.0:
        status = "Fever"
    else:
        status = "High fever"
        
    # Друкуємо рядок таблиці з фіксованою шириною колонок
    print(f"{index:<3} {name:<9} {temp:<13.1f} {status}")

"""
📋 Результат у консолі:
#   Patient   Temperature   Status
1   Ivan      36.6          Normal
2   Olena     38.2          Fever
3   Petro     39.1          High fever
4   Hanna     37.0          Normal

🧠 Розбір польотів (Що тут відбувається?):
enumerate(patients, start=1)
Зазвичай enumerate починає рахувати з нуля. Але оскільки ми виводимо список для людей (номери пацієнтів), ми передаємо аргумент start=1. Це позбавляє нас необхідності писати костилі на кшталт index + 1 при виводі.

Розпакування кортежу (Tuple unpacking)
Зверніть увагу на конструкцію for index, (name, temp) in...
Оскільки кожен елемент списку — це кортеж (наприклад, ("Ivan", 36.6)), ми можемо одразу "розпакувати" його у дві зручні змінні name та temp, замість того щоб звертатися за індексами patient[0] та patient[1]. Це робить код набагато читабельнішим.

Вирівнювання у f-strings (:<9)
Щоб колонки не з'їжджали, ми вказуємо ширину поля:

:<3 означає: виділи 3 символи і вирівняй текст по лівому краю.

temp:<13.1f — це подвійне комбо! Виділяємо 13 символів з вирівнюванням ліворуч (<13) і гарантуємо, що число матиме рівно 1 знак після коми (.1f).
"""

# 🎨 Фірмова версія з рамкою для дашборду:

patients = [
    ("Ivan", 36.6),
    ("Olena", 38.2),
    ("Petro", 39.1),
    ("Hanna", 37.0),
]

print("\n┌───┬───────────┬───────────────┬──────────────┐")
print("│ # │ Patient   │ Temperature   │ Status       │")
print("├───┼───────────┼───────────────┼──────────────┤")

for index, (name, temp) in enumerate(patients, start=1):
    if temp < 37.5:
        status = "✅ Normal"
    elif temp < 39.0:
        status = "⚠️ Fever"
    else:
        status = "🚨 High fever"
        
    print(f"│ {index:<1} │ {name:<9} │ {temp:<13.1f} │ {status:<12} │")

print("└───┴───────────┴───────────────┴──────────────┘")


"""
┌───┬───────────┬───────────────┬──────────────┐
│ # │ Patient   │ Temperature   │ Status       │
├───┼───────────┼───────────────┼──────────────┤
│ 1 │ Ivan      │ 36.6          │ ✅ Normal    │
│ 2 │ Olena     │ 38.2          │ ⚠️ Fever     │
│ 3 │ Petro     │ 39.1          │ 🚨 High fever│
│ 4 │ Hanna     │ 37.0          │ ✅ Normal    │
└───┴───────────┴───────────────┴──────────────┘
"""

# Variant #2

patients = [
    ("Ivan", 36.6),
    ("Olena", 38.2),
    ("Petro", 39.1),
    ("Hanna", 37.0),
]

MARKER = {
    "Normal":     "🟢",
    "Fever":      "🟡",
    "High fever": "🔴",
}

# --- Формуємо рядки таблиці ---
rows = []

# enumerate() дає (номер, елемент), а елемент — це tuple (ім'я, температура)
# розпаковуємо ОБИДВА рівні одразу: (index, (name, temp))
for index, (name, temp) in enumerate(patients, start=1):

    # if/elif/else — визначаємо статус (межі без перекриттів)
    if temp < 37.5:
        status = "Normal"
    elif temp < 39.0:      # Python вже знає: temp >= 37.5
        status = "Fever"
    else:                  # temp >= 39.0
        status = "High fever"

    marker = MARKER[status]

    # f-string з вирівнюванням через :< (ліворуч) і задану ширину
    row = f"{index:<3} │ {name:<8} │ {temp:<5.1f} │ {marker} {status}"
    rows.append(row)

# --- Заголовок таблиці (та сама структура колонок) ---
header = f"{'#':<3} │ {'Patient':<8} │ {'Temp':<5} │ Status"

# --- Вивід у рамці ---
all_lines = [header] + rows
width = max(len(line) for line in all_lines) + 4

print("\n┌" + "─" * width + "┐")
print("│" + "  TEMPERATURE REPORT".center(width) + "│")
print("├" + "─" * width + "┤")
print("│  " + header.ljust(width - 2) + "│")
print("│  " + "─" * (width - 4) + "│")
for row in rows:
    print("│  " + row.ljust(width - 2) + "│")
print("└" + "─" * width + "┘")

"""
Розбір кожної технології, яку об'єднує це завдання:

1. enumerate() + розпакування вкладеного tuple:

patients = [("Ivan", 36.6), ("Olena", 38.2), ...]

for index, (name, temp) in enumerate(patients, start=1):
#    ↑        ↑     ↑
#  номер   ім'я  температура

Тут відбувається подвійне розпакування: enumerate() дає (1, ("Ivan", 36.6)), а потім (name, temp) розпаковує внутрішній tuple ("Ivan", 36.6) ще раз — усе в одному рядку for.

2. if/elif/else — класифікація за межами без перекриттів (як у попередніх завданнях з BMI/температурою).

3. f-string з форматуванням вирівнювання:

f"{index:<3}"    # <  — вирівняти ЛІВОРУЧ, ширина поля 3 символи
f"{temp:<5.1f}"  # <5.1f — ліворуч, ширина 5, 1 знак після коми

Символ < (ліворуч), > (праворуч), ^ (по центру) — вказує напрямок вирівнювання всередині заданої ширини поля.

4. Словник MARKER — той самий підхід, що й у минулих завданнях: замість if status == "Fever": marker = "🟡" (ще один if/elif) — просто MARKER[status], бо словник уже містить готове відображення.

Приклад виводу:

┌───────────────────────────────────────┐
│           TEMPERATURE REPORT              │
├───────────────────────────────────────┤
│  #   │ Patient  │ Temp  │ Status         │
│  ─────────────────────────────────       │
│  1   │ Ivan     │ 36.6  │ 🟢 Normal      │
│  2   │ Olena    │ 38.2  │ 🟡 Fever       │
│  3   │ Petro    │ 39.1  │ 🔴 High fever  │
│  4   │ Hanna    │ 37.0  │ 🟢 Normal      │
└───────────────────────────────────────┘
"""

# ==============================================================================
# ==============================================================================

## 🚀 Challenge №6 — логіка пошуку

"""
Є:
patients = [
    "Ivan",
    "Olena",
    "Petro",
    "Hanna",
]

Користувач вводить ім'я.

Програма повинна:
пройти список;
знайти пацієнта;
вивести "Patient found";
використати break;
якщо пацієнта немає — вивести "Patient not found".

Підказка: тут дуже добре підходить for ... else.
"""

# Variant #1
"""
Ця задача — просто ідеальний майданчик для демонстрації магії конструкції for ... else. У багатьох інших мовах програмування довелося б створювати додаткову змінну-прапорець (наприклад, is_found = False), але Python дозволяє зробити це елегантно.
"""

#💻 Код програми:
patients = ["Ivan", "Olena", "Petro", "Hanna"]

# Імітуємо ввід користувача (у реальній програмі тут був би input())
search_name = "Petro" 
print(f"🔍 Шукаємо пацієнта: {search_name}...\n")

for patient in patients:
    if patient == search_name:
        print("✅ Patient found")
        break  # Знайшли — негайно зупиняємо цикл!
else:
    # Цей блок виконається ТІЛЬКИ якщо цикл пройшов до кінця і не зустрів break
    print("❌ Patient not found")

"""
⚙️ Покрокове виконання (Сценарій 1: Пацієнт Є у списку)
Шукаємо "Petro":
# Крок 1: patient = "Ivan"  → "Ivan" == "Petro"? Ні.
# Крок 2: patient = "Olena" → "Olena" == "Petro"? Ні.
# Крок 3: patient = "Petro" → "Petro" == "Petro"? ТАК! 
#         → print("Patient found") 
#         → break (цикл переривається, блок else ігнорується)

⚙️ Покрокове виконання (Сценарій 2: Пацієнта НЕМАЄ у списку)
Шукаємо "Dmytro":
# Крок 1: patient = "Ivan"  → "Ivan" == "Dmytro"? Ні.
# Крок 2: patient = "Olena" → "Olena" == "Dmytro"? Ні.
# Крок 3: patient = "Petro" → "Petro" == "Dmytro"? Ні.
# Крок 4: patient = "Hanna" → "Hanna" == "Dmytro"? Ні.
# Цикл завершив роботу природним шляхом (без break).
# Спрацьовує блок else: print("Patient not found")

⚖️ Порівняння: for...else vs Традиційний підхід
Ось чому підхід з else крутіший. 
Погляньте, як би ми писали це без нього (так роблять у C++ чи Java):
"""

# ⚠️ Традиційний підхід (змінна-прапорець):
found = False                      # 1. Створюємо прапорець
for patient in patients:
    if patient == search_name:
        print("✅ Patient found")
        found = True               # 2. Змінюємо прапорець
        break

if not found:                      # 3. Окремо перевіряємо прапорець
    print("❌ Patient not found")

"""
Завдяки for...else ми позбулися зайвої змінної found та зайвої перевірки if після циклу. Код став коротшим і більш "pythonic".
"""

# 📊 Фірмовий вивід у рамці (Система пошуку):
# 💻 Інтерактивний код:
    
patients = ["Ivan", "Olena", "Petro", "Hanna"]
WIDTH = 44 

print("👋 Вітаємо в системі пошуку! (Натисніть Enter без вводу, щоб вийти)\n")

while True:
    # Запитуємо ввід користувача та очищаємо від зайвих пробілів
    search_name = input("Введіть ім'я пацієнта: ").strip()
    
    # Умова виходу: якщо нічого не ввели (просто натиснули Enter)
    if not search_name:
        print("Вихід із системи. Гарного дня! 🏥")
        break

    # 1. Логіка пошуку (наш for...else)
    for patient in patients:
        # Для зручності можна додати .lower() до обох змінних, щоб пошук не залежав від регістру:
        # if patient.lower() == search_name.lower():
        if patient == search_name:
            status_msg = "✅ Patient found"
            break
    else:
        status_msg = "❌ Patient not found"

    # 2. Формування та вивід рамки
    print("┌" + "─" * WIDTH + "┐")
    print("│" + "ПОШУК В БАЗІ ПАЦІЄНТІВ".center(WIDTH) + "│")
    print("├" + "─" * WIDTH + "┤")
    
    # Готуємо рядки з відступами зліва
    query_line = f"  Запит: {search_name}"
    status_line = f"  Статус: {status_msg}"
    
    # Виводимо їх, доповнюючи пробілами до потрібної ширини (ljust)
    print("│" + query_line.ljust(WIDTH) + "│")
    print("│" + status_line.ljust(WIDTH) + "│")
    
    print("└" + "─" * WIDTH + "┘\n")

"""
🧠 :
while True: створює інтерактивний режим — програма не завершиться після одного пошуку.

input().strip() зчитує текст з клавіатури. Метод .strip() дуже корисний, бо він видаляє випадкові пробіли на початку або в кінці (якщо ви випадково ввели " Petro ", програма побачить "Petro").

if not search_name: break — це запобіжник, який зупиняє роботу, якщо змінна порожня. Без цього вам довелося б закривати термінал хрестиком або тиснути Ctrl+C.
"""

"""
┌────────────────────────────────────────────┐
│          ПОШУК В БАЗІ ПАЦІЄНТІВ            │
├────────────────────────────────────────────┤
│  Запит: Petro                              │
│  Статус: ✅ Patient found                  │
└────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│          ПОШУК В БАЗІ ПАЦІЄНТІВ            │
├────────────────────────────────────────────┤
│  Запит: Dmytro                             │
│  Статус: ❌ Patient not found              │
└────────────────────────────────────────────┘
"""

# Variant #2

patients = [
    "Ivan",
    "Olena",
    "Petro",
    "Hanna",
]

search_name = input("Введіть ім'я пацієнта для пошуку: ").strip()

steps = []   # кроки пошуку для виводу в рамці

# --- for/else: ідеальна конструкція саме для пошуку ---
for patient in patients:
    # .lower() з обох боків — порівняння нечутливе до регістру
    # "ivan" == "Ivan" було б False, але "ivan" == "ivan" (обидва lower) — True
    if patient.lower() == search_name.lower():
        steps.append(f"✅ {patient} ← знайдено!")
        result = "Patient found"
        break                              # зупиняємо цикл одразу
    steps.append(f"🔍 {patient} — не збігається")
else:
    # виконується ТІЛЬКИ якщо break НЕ спрацював (пройшли весь список)
    result = "Patient not found"

# --- Вивід у рамці ---
lines = steps + ["─" * max(len(s) for s in steps), result]
width = max(len(line) for line in lines) + 4

print("\n┌" + "─" * width + "┐")
print("│" + f"  ПОШУК: {search_name}".center(width) + "│")
print("├" + "─" * width + "┤")
for line in lines:
    print("│  " + line.ljust(width - 2) + "│")
print("└" + "─" * width + "┘")

"""
Чому for...else тут ідеально підходить:
for patient in patients:
    if patient.lower() == search_name.lower():
        result = "Patient found"
        break            # ← знайшли → else НЕ виконається
else:
    result = "Patient not found"   # ← виконається ЛИШЕ якщо break не спрацював

Це рівно та логіка, яку описує підказка в завданні: else тут означає "якщо цикл дійшов до кінця, так і не знайшовши збігу" — саме сценарій "not found".

Чому .lower() з обох сторін:
patient = "Ivan"
search_name = "ivan"

patient == search_name              # False — різний регістр!
patient.lower() == search_name.lower()   # True  — "ivan" == "ivan" ✅

Важливо порівнювати обидві сторони через .lower(), а не тільки одну — бо якщо порівняти "Ivan".lower() з просто search_name (без .lower()), пошук "IVAN" все одно не спрацює.
"""

#Альтернатива без for/else (для порівняння):

# ❌ Без for/else потрібен окремий прапорець:
found = False
for patient in patients:
    if patient.lower() == search_name.lower():
        found = True
        break
result = "Patient found" if found else "Patient not found"

# ✅ З for/else — прапорець не потрібен, код чистіший:
for patient in patients:
    if patient.lower() == search_name.lower():
        result = "Patient found"
        break
else:
    result = "Patient not found"

"""
Приклад роботи (пошук "petro" малими літерами):
Введіть ім'я пацієнта для пошуку: petro

┌─────────────────────────┐
│      ПОШУК: petro         │
├─────────────────────────┤
│  🔍 Ivan — не збігається  │
│  🔍 Olena — не збігається │
│  ✅ Petro ← знайдено!     │
│  ────────────────────     │
│  Patient found             │
└─────────────────────────┘
"""

"""
Введіть ім'я пацієнта для пошуку: olena

┌──────────────────────────┐
│        ПОШУК: olena      │
├──────────────────────────┤
│  🔍 Ivan — не збігається  │
│  ✅ Olena ← знайдено!     │
│  ──────────────────────  │
│  Patient found           │
└──────────────────────────┘
"""

"""
Введіть ім'я пацієнта для пошуку: Olena

┌──────────────────────────┐
│        ПОШУК: Olena      │
├──────────────────────────┤
│  🔍 Ivan — не збігається  │
│  ✅ Olena ← знайдено!     │
│  ──────────────────────  │
│  Patient found           │
└──────────────────────────┘
"""

"""
Введіть ім'я пацієнта для пошуку: Dinis

┌───────────────────────────┐
│         ПОШУК: Dinis      │
├───────────────────────────┤
│  🔍 Ivan — не збігається   │
│  🔍 Olena — не збігається  │
│  🔍 Petro — не збігається  │
│  🔍 Hanna — не збігається  │
│  ───────────────────────  │
│  Patient not found        │
└───────────────────────────┘
"""
